/**
 * 전역 타입 정의 - 뼈대
 * DB 스키마 타입은 supabase gen types 로 생성 후 database.gen.ts 에 두거나 여기서 re-export
 */

// 예시 (실제 테이블명에 맞게 수정)
// export type Project = { id: string; name: string; ... };
// export type Student = { id: string; project_id: string; ... };
